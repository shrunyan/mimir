app.Views.noteEdit = Backbone.View.extend({
	tag: 'form',
	template: _.template(),
	events: {
		'keyup form': 'save'
	},
	initalize: function initalize() {

	}, 
	render: function render() {
		
	},
	save: function save() {

	}
});