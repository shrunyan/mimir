"use strict"

import db from 'db'

/**
  Note Controller
  Fetches a single note model from the database to be returned
*/
export default function noteController (req, res) {

  // Request note ID from mongo

  // Check for record

    // return note

    // return error: not found

    res.status(200).json({
      success: 'SUCCESS'
    })

}
